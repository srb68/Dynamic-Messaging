🤖 Integrated Intelligent Chatbot

The system now features a real-time support interface designed to assist caterers with system navigation and FAQs.Real-Time AJAX Communication: Developed an asynchronous pipeline between the client-side UI (chatbot.html) and the server-side logic (chat.php), enabling seamless interaction without page reloads.Session-Aware Security: Integrated a session-check layer (check_session.php) that ensures only authenticated caterers can access the support portal.JSON Response Handling: Implemented structured data exchange using JSON for efficient server-to-client communication.Dynamic UI Management: Built a responsive chat window with auto-scrolling and real-time message injection using Vanilla JavaScript.📂 New File LogicFilePurposechatbot.htmlThe frontend chat interface, featuring dynamic message rendering and AJAX triggers.chat.phpThe backend processing engine that handles user input and determines automated responses.auth.php / check_session.phpSecurity middleware that validates the active caterer session before allowing chat interactions.users.phpHandles data operations and server-side logic related to user status and profile interactions.
